'use strict';

angular.
  module('mainMenu').
  component('mainMenu', {
    templateUrl: 'main-menu/main-menu.template.html',
    controller: ['$http', function MainMenuController($http) {
      
    }]
  });
